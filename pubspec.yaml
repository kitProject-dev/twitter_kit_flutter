name: twitter_kit
description: A new Flutter plugin.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^3.0.1
  chopper: ^3.0.2
  oauth1: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.8.0
  json_serializable: ^3.3.0
  chopper_generator: ^3.0.4

flutter:
  plugin:
    androidPackage: info.kitproject.twitter_kit
    pluginClass: TwitterKitPlugin
  assets:
    - assets/test/json/